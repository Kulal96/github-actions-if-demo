name: 'Reusable Workflow Deploy'

on:
    workflow_call:

jobs:
    reusable_workflow_job:
        runs-on: ubuntu-latest
        steps:
            - name: Deploy
              run: echo "Deploying..."
